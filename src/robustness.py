from __future__ import annotations

import pandas as pd


def summarize_missing_modality(results: pd.DataFrame) -> pd.DataFrame:
    """Return the missing-modality result table unchanged for reporting."""
    return results

